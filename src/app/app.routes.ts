import { Routes } from '@angular/router';
import { ProduitEpargneComponent } from './epargne-pension/produit-epargne.component';

export const routes: Routes = [
  { path: '', component: ProduitEpargneComponent }
];
